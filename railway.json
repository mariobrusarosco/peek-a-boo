{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm build:shared && pnpm build:sdk-service"
  },
  "deploy": {
    "startCommand": "cd apps/sdk-service && pnpm start:prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "on_failure"
  }
} 